FROM python:3.12-slim

WORKDIR /app

# Copy FastAPI app
COPY app /app/app

# Copy dependencies
COPY requirements.txt /app

RUN pip install --no-cache-dir -r requirements.txt

# app/app.py contains: app = FastAPI()
CMD ["uvicorn", "app.app:app", "--host", "0.0.0.0", "--port", "8000"]
